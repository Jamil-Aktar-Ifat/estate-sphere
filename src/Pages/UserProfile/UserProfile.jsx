import DynamicTitle from "../../components/DynamicTitle/DynamicTitle";

const UserProfile = () => {
  DynamicTitle("User Profile");
  return (
    <div>
      <h2 className="text-center">This is user profile</h2>
    </div>
  );
};

export default UserProfile;
